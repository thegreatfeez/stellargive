#!/bin/bash
# Script to deploy the Soroban contract
echo "Deploying contract..."
# stellar contract deploy --wasm target/wasm32-unknown-unknown/release/stellar_give.wasm --source-account <ACCOUNT> --network testnet
